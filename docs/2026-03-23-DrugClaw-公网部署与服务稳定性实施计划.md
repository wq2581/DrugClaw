# DrugClaw 公网部署与服务稳定性实施计划

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不改动 DrugClaw 主查询链路的前提下，修复服务层两个稳定性问题，并补齐最小可用的公网部署模板与中文部署文档。

**Architecture:** 继续把 `DrugClawSystem.query()` 作为唯一主查询入口，只在 `server_models.py`、`service_runtime.py`、`server_app.py` 中修复请求默认值和并发释放逻辑。公网访问仍采用 `nginx -> 127.0.0.1:8000 -> DrugClaw` 的保守部署方式，并通过 `systemd` 守护 Python 服务。

**Tech Stack:** Python, FastAPI, pytest, nginx, systemd, Markdown

---

## File Map

**Create**

- `deploy/nginx/drugclaw.conf`
- `deploy/systemd/drugclaw.service`
- `docs/2026-03-23-DrugClaw-公网部署说明.md`

**Modify**

- `drugclaw/server_models.py`
- `drugclaw/service_runtime.py`
- `drugclaw/server_app.py`
- `tests/server/test_service_runtime.py`
- `tests/server/test_server_api.py`
- `README.md`
- `README_CN.md`

---

## Chunk 1: 服务层稳定性修复

### Task 1: 修复 timeout 后并发槽位提前释放问题

**Files:**

- Modify: `tests/server/test_service_runtime.py`
- Modify: `drugclaw/service_runtime.py`

- [ ] 新增失败测试，覆盖“超时后槽位仍被占用，直到后台任务真正结束”
- [ ] 运行 `pytest tests/server/test_service_runtime.py -q`，确认先红
- [ ] 最小修改 `service_runtime.py`，把槽位释放绑定到 `future` 完成回调
- [ ] 再次运行 `pytest tests/server/test_service_runtime.py -q`，确认转绿

### Task 2: 修复 omitted mode 未使用服务默认模式问题

**Files:**

- Modify: `tests/server/test_server_api.py`
- Modify: `drugclaw/server_models.py`
- Modify: `drugclaw/server_app.py`

- [ ] 新增失败测试，覆盖“请求未传 `mode` 时使用 `SERVER_DEFAULT_MODE`”
- [ ] 运行 `pytest tests/server/test_server_api.py -q`，确认先红
- [ ] 最小修改请求模型和 API 层回退逻辑
- [ ] 再次运行 `pytest tests/server/test_server_api.py -q`，确认转绿

## Chunk 2: 公网部署资产

### Task 3: 增加部署模板

**Files:**

- Create: `deploy/nginx/drugclaw.conf`
- Create: `deploy/systemd/drugclaw.service`

- [ ] 增加基于反向代理的 nginx 配置模板
- [ ] 增加 systemd 守护模板
- [ ] 模板中明确只反代到 `127.0.0.1:8000`

### Task 4: 增加中文部署文档与 README 指引

**Files:**

- Create: `docs/2026-03-23-DrugClaw-公网部署说明.md`
- Modify: `README.md`
- Modify: `README_CN.md`

- [ ] 写清公网部署拓扑、HTTPS、systemd、nginx、防火墙建议
- [ ] 在 README 中增加指向部署文档和模板目录的入口
- [ ] 明确说明不要直接裸暴露 `8000`

## Chunk 3: 回归验证

### Task 5: 运行回归

**Files:**

- Verify only

- [ ] 运行 `pytest tests/server/test_service_runtime.py tests/server/test_server_api.py -q`
- [ ] 运行 `python3 -m pytest -q`
- [ ] 检查 `git status --short`
- [ ] 只有在测试通过后再汇报完成
