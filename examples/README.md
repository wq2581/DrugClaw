# Examples

This directory contains optional runnable examples and compatibility entrypoints.

For a first run, prefer the supported CLI flows from the repository root:

```bash
python -m drugclaw list
python -m drugclaw doctor
python -m drugclaw demo
```

Use the scripts here when you want extra sample code or a lightweight wrapper around the CLI.
